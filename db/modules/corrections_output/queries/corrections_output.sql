/*
Corrections Output bootstrap.

This app reads correction rows from corrections_* tables and writes SQL scripts
from the application layer. No schema actions are required here.
*/
SELECT 1 AS corrections_output_ready;
